import React, { FC } from 'react';
import { Footer, Header, Main } from 'components';

const App: FC = () => (
	<div className="app">
		<Header />
		<Main />
		<Footer />
	</div>
);

export default App;
